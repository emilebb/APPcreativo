export const metadata = {
  title: 'Login - CreacionX',
  description: 'Inicia sesión en tu plataforma creativa',
};

export const viewport = {
  themeColor: '#a855f7',
  width: 'device-width',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
